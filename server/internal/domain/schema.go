package domain

// CurrentSchemaVersion is the version stamped on every cached artifact and on
// every Graph emitted by the analysis pipeline. Bump it whenever an on-disk or
// over-the-wire structure changes in a backwards-incompatible way.
const CurrentSchemaVersion = 1
